{
  int k; int sum, input, i;
  sum = 0;
  for i=0; to 3^144 by k+1 while(sum<1000){
		    read(input);
		  if (input > 0)
		  {
		    sum = sum + input;
		    write(sum);
		  }
		
	}rof;
  sum = sum^2;
  write(sum);
}

